# ==============================================================================
# EKMP-C4 Architecture Visualization Environment - Configuration
# ==============================================================================
# Repository : https://github.com/JoZapf/EKMP-C4-arch-containerized-visualization-environment
# License    : MIT
# ==============================================================================
# Copy this file to .env and adjust values for your environment.
# ==============================================================================

# =============================================================================
# Base Domain / Hostname
# =============================================================================
# For local development : arch.local  (must be added to /etc/hosts or hosts file)
# For LAN deployment    : e.g. arch.crea-think.lan (resolved via DNS/dnsmasq)
# For production        : your actual domain
ARCH_BASE_DOMAIN=arch.local

# =============================================================================
# Port Configuration
# =============================================================================
# HTTP port for all services routed through Traefik (default: 80)
HTTP_PORT=80

# HTTPS port (default: 443 - not yet configured, reserved for future TLS)
HTTPS_PORT=443

# Traefik Dashboard port (default: 9090)
# Access at: http://localhost:9090
TRAEFIK_DASHBOARD_PORT=9090

# =============================================================================
# Paths
# =============================================================================
# Path to the architecture repository (relative to docker-compose.yml)
# Must contain: docs/, c4/, assets/
ARCH_REPO_PATH=./repo

# =============================================================================
# PlantUML Configuration
# =============================================================================
# Maximum image size for PlantUML rendering (default: 8192)
PLANTUML_LIMIT_SIZE=8192

# =============================================================================
# Traefik Configuration
# =============================================================================
# Log level for Traefik reverse proxy (DEBUG, INFO, WARN, ERROR)
TRAEFIK_LOG_LEVEL=INFO

# =============================================================================
# Authentication (optional - not yet enabled)
# =============================================================================
# Basic Auth for admin areas
# Generate hash with: htpasswd -nb admin changeme
# IMPORTANT: Replace $ with $$ for docker-compose!
# BASIC_AUTH_USER=admin
# BASIC_AUTH_PASSWORD_HASH=admin:$$apr1$$xyz...

# =============================================================================
# TLS / HTTPS (optional - requires additional Traefik config)
# =============================================================================
# ACME_EMAIL=your-email@example.com
