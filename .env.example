# EMPC4 VIS Stack - Environment Configuration
# Kopiere diese Datei zu .env und passe die Werte an

# =============================================================================
# Basis-Domain / Hostname
# =============================================================================
# Für lokale Entwicklung: arch.local (muss in /etc/hosts eingetragen werden)
# Für Produktion: deine tatsächliche Domain
ARCH_BASE_DOMAIN=arch.local

# =============================================================================
# Port-Konfiguration
# =============================================================================
# HTTP-Port für alle Services (Standard: 80)
HTTP_PORT=80

# HTTPS-Port (Standard: 443, noch nicht konfiguriert)
HTTPS_PORT=443

# Traefik Dashboard Port (Standard: 8080)
# Zugriff auf: http://localhost:8080
TRAEFIK_DASHBOARD_PORT=8080

# =============================================================================
# Pfade
# =============================================================================
# Pfad zum Architektur-Repository (relativ zu docker-compose.yml)
# Muss docs/, c4/, assets/ enthalten
ARCH_REPO_PATH=./repo

# =============================================================================
# Authentication (Optional, noch nicht aktiviert)
# =============================================================================
# Basic Auth für Admin-Bereiche
# Hash generieren mit: htpasswd -nb admin changeme
# WICHTIG: Ersetze $ durch $$ für docker-compose!
# BASIC_AUTH_USER=admin
# BASIC_AUTH_PASSWORD_HASH=admin:$$apr1$$xyz...

# =============================================================================
# PlantUML-Konfiguration
# =============================================================================
# Maximale Bildgröße für PlantUML (Standard: 4096)
PLANTUML_LIMIT_SIZE=8192

# =============================================================================
# Erweiterte Konfiguration (Optional)
# =============================================================================
# TLS/HTTPS (erfordert zusätzliche Traefik-Konfiguration)
# ACME_EMAIL=your-email@example.com
